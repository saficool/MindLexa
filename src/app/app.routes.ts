import { Routes } from '@angular/router';
import { RelationGraphComponent } from './pages/relation-graph/relation-graph.component';

export const routes: Routes = [
    { path: '', component: RelationGraphComponent }
];
